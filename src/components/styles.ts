import { css } from '@emotion/css';
import { green } from '@ant-design/colors';

export const styContent = css`
  background: ${green[0]};
  height: 100%;
  padding: 12px;
`;

export const styFullWidth = css`
  width: 100%;
`;
